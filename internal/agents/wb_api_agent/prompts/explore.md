Explore the WB OpenAPI registry and select only valid operations.

Prefer readonly operations when readonly_only=true.
Exclude Jam-only operations when no_jam_subscription=true.

Do not use operation names that are not present in the registry.
A generated operation_id is valid only if it exists in the registry.